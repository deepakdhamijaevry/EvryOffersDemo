export enum ControlType {
    desc = 1,
    est = 2,
    img = 3,
    timeline = 4,
    title = 5,
    subtitle = 6,
    documents = 7
}

export enum FILE_TYPES {
    PDF = 'pdf',
    IMG = 'img',
    CSV = 'csv'
}